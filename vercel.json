{
  "buildCommand": "cd project-00022 && npm install && npm run build",
  "outputDirectory": "project-00022/dist",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}
